
class ScoreCalculator:
    def __init__(self, db):
        self.db = db

    def calc_score(self, name, packet):
        """
        점수를 계산해서 반환함.
        """
    
    def calc_flood(packet):
        """
        flood 전용 점수 계산기
        """
        # counter에 따라서 점수 계산해서 반환
    
    def get_counter(name, src_ip):
        """
        공격 이름과 src_ip로 찾아서 counter 반환
        """